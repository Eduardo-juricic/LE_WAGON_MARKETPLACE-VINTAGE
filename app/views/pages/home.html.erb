<h1>Bem-vindo ao New Again!</h1>
<p>O melhor lugar para comprar e vender roupas novas ou semi-novas.</p>

<% if user_signed_in? %>
  <div class="cta">
    <%= link_to 'Cadastrar Novo Produto', new_product_path, class: 'btn btn-success' %>
  </div>
<% else %>
  <div class="cta">
    <p>Cadastre-se ou faça login para começar a vender seus produtos.</p>
    <%= link_to 'Cadastrar-se', new_user_registration_path, class: 'btn btn-primary' %> |
    <%= link_to 'Entrar', new_user_session_path, class: 'btn btn-secondary' %>
  </div>
<% end %>

<h2>Produtos em Destaque</h2>

<div class="products-list">
  <% @products.each do |product| %>
    <div class="product-card">
      <h3><%= product.name %></h3>
      <p><%= product.description %></p>
      <p><strong>Preço:</strong> R$ <%= product.price %></p>
      <p><strong>Categoria:</strong> <%= product.category %></p>
      <p><strong>Tamanho:</strong> <%= product.size %></p>
      <%= link_to 'Ver Produto', product_path(product), class: 'btn btn-info' %>
      <%= link_to 'Comprar', new_order_path(product_id: product.id), class: 'btn btn-primary' %>
    </div>
  <% end %>
</div>
